import { useState } from "react";
import { HeartHandshake, Phone, ShieldAlert, UserRoundCheck } from "lucide-react";
import { AppShell } from "./app-shell"; import { PageHeader } from "./page-header"; import { Button } from "@/components/ui/button"; import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { mindtrailService } from "@/services/mindtrail";
export function SupportScreen(){const [choice,setChoice]=useState<"peer"|"counsellor"|null>(null);const [sent,setSent]=useState(false);const[busy,setBusy]=useState(false);const[error,setError]=useState("");const confirm=async()=>{if(!choice)return;setBusy(true);setError("");try{await mindtrailService.requestSupport(choice);setSent(true)}catch{setError("The request could not be recorded. Please try again.")}finally{setBusy(false)}};return <AppShell><div className="mx-auto max-w-5xl px-5 py-8 sm:px-8 lg:py-12"><PageHeader eyebrow="Your choice" title="Support, when you want it." description="Explore fictional demo resources. Nothing is contacted or shared until you clearly confirm."/><div className="mt-8 grid gap-5 md:grid-cols-2"><div className="border bg-card p-6"><HeartHandshake className="size-6 text-primary"/><h2 className="mt-6 font-display text-2xl font-semibold">Peer listening</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">Talk with a trained student listener about campus life, study pressure, or simply having a difficult day.</p><ul className="mt-5 space-y-2 text-sm"><li>Available weekdays, 4–9 PM</li><li>Student-led and confidential</li><li>You can stop at any time</li></ul><Button className="mt-6" variant="outline" onClick={()=>{setSent(false);setChoice("peer")}}><UserRoundCheck/>Request peer support</Button></div><div className="border bg-card p-6"><ShieldAlert className="size-6 text-warm"/><h2 className="mt-6 font-display text-2xl font-semibold">College wellbeing team</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">Ask the fictional campus wellbeing desk to contact you about a private appointment.</p><ul className="mt-5 space-y-2 text-sm"><li>Weekdays, 9 AM–5 PM</li><li>Professionally supported</li><li>You decide what context to share</li></ul><Button className="mt-6" onClick={()=>{setSent(false);setChoice("counsellor")}}><Phone/>Ask for a call</Button></div></div><div className="mt-5 border border-warm/40 bg-warm/10 p-6"><h2 className="font-display text-xl font-semibold">Need urgent help now?</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">If you or someone else may be in immediate danger, contact local emergency services or go to the nearest emergency department. This demo does not monitor messages in real time.</p></div><Dialog open={choice!==null} onOpenChange={(open)=>!open&&setChoice(null)}><DialogContent><DialogHeader><DialogTitle>{sent?"Request recorded":"Confirm this next step"}</DialogTitle><DialogDescription>{sent?"This is a simulation. No real person was contacted.":`You are choosing the fictional ${choice==="peer"?"peer listening service":"college wellbeing team"}. Only your name and callback preference would be shared.`}</DialogDescription></DialogHeader>{error&&<p role="alert" className="text-sm text-destructive">{error}</p>}{!sent&&<DialogFooter><Button variant="outline" onClick={()=>setChoice(null)}>Cancel</Button><Button disabled={busy} onClick={confirm}>{busy?"Recording…":"I agree, continue"}</Button></DialogFooter>}</DialogContent></Dialog></div></AppShell>}